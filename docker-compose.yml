services:
  db:
    image: postgres
    volumes:
      - postgres-db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=medusa_db
      - POSTGRES_USER=inert
      - POSTGRES_PASSWORD=inert
    ports:
      - 5432:5432
volumes:
  postgres-db:
